image:
  file: .gitpod.Dockerfile

tasks:
  - init: npm install && npm run build
    command: npm run start

# tasks:
#   - init: npm install
#     command: npm start

# ports:
#   - port: 3000
#     onOpen: open-preview